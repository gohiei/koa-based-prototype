/**
 * @typedef {import('koa')} KoaApp
 * @typedef {import('koa').Context} KoaContext
 * @typedef {import('koa').Request} KoaRequest
 * @typedef {import('koa').Response} KoaResponse
 * @typedef {import('koa').Middleware} KoaMiddleware
 */
